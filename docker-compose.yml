# Deprecated — use role-specific compose files instead:
#   Server:  docker compose -f docker-compose.server.yml up -d
#   Desktop: docker compose -f docker-compose.client.yml up -d
#
# Or run the setup scripts:
#   ./scripts/server-deploy.sh
#   ./scripts/desktop-client-setup.sh --server-url http://SERVER:8000
