# Use role-specific Dockerfiles:
#   Dockerfile.server  — central server (API, WhatsApp, DB)
#   Dockerfile.client  — desktop capture (Screenpipe, OCR)
#
# See docker-compose.server.yml and docker-compose.client.yml
